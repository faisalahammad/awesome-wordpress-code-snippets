/**
 * Ninja Forms - Change Field Type to URL
 * @author Faisal Ahammad
 */

jQuery(document).ready(function ($) {
  setTimeout(function () {
    $("input.websiteurl").attr("type", "url");
  }, 500);
});
